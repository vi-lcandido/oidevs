import React from "react";

const Header = () => {
  return (
    <header>
      <button>
        <a href="/">Home</a>
      </button>

      <button>
        <a href="/produtos">Produtos</a>
      </button>
    </header>
  );
};

export default Header;
